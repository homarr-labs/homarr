---
title: Homarr provider
sidebar_position: 2
tags:
  - Assistant
  - AI
  - Privacy
  - Community
---

import { AssistantProviderActivity } from "@site/src/components/assistant-provider/assistant-provider-activity";
import { OpenCollectiveLink } from "@site/src/components/assistant-provider/open-collective-link";

# Homarr provider

The Homarr provider gives Workshop users access to **DeepSeek V4 Flash Latest** without an administrator API key.
Each user signs in with GitHub and receives a separate daily allowance.

![Homarr provider configuration](/img/assistant/homarr-provider.png)

## Set it up

1. Open **Management → Assistant**.
2. Select **Homarr** as the provider.
3. Save the provider connection.
4. Select the available model, enable the assistant, and save.
5. Open the assistant and sign in to the Workshop when asked.

## Daily allowance

- Each user starts with **50 request units per UTC day**.
- A model request uses one unit.
- Each completed Homarr tool call uses one additional unit.
- The allowance resets at 00:00 UTC.

The battery beside the model selector shows the remaining allowance and reset time. The Workshop server enforces the
limit before it sends a request to OpenRouter.

<AssistantProviderActivity />

## Data and cost

The Workshop keeps the OpenRouter API key on its server. It does not send that key to Homarr or the browser.

Prompts, attachments, tool definitions, and tool results are sent to OpenRouter to generate a response. The Workshop
does not store this content. It stores only request status, model, usage, duration, cost, and timestamps.

The Homarr community pays for these requests. Donations help cover the cost.

<OpenCollectiveLink />

Workshop operators can change the upstream, default allowance, and security limits in the
[Workshop operator guide](./operator#homarr-provider-proxy).
