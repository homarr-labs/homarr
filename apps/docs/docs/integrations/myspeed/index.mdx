---
title: "MySpeed"
description: "A self-hosted application for tracking internet speed tests."
hide_title: true
---

import { IntegrationHeader } from "@site/src/components/integrations/header";
import { IntegrationCapabilites } from "@site/src/components/integrations/widgets";
import { AddingIntegration } from "@site/src/components/integrations/adding";
import { IntegrationSecrets } from "@site/src/components/integrations/secrets";
import { myspeedIntegration } from ".";
import { statsWidget } from "@site/docs/widgets/stats";

<IntegrationHeader integration={myspeedIntegration} categories={["Speed test"]} />

### Widgets & Capabilities

<IntegrationCapabilites items={[{ widget: statsWidget }]} />

The [Stats widget](/docs/widgets/stats) displays the latest ping, download, and upload results from MySpeed.

### Adding the integration

<AddingIntegration />

### Secrets

<IntegrationSecrets
  secrets={[
    {
      credentials: ["password"],
      steps: [
        "If your MySpeed instance is password protected, enter the same password in the Password field when creating the integration in Homarr.",
      ],
    },
  ]}
/>
