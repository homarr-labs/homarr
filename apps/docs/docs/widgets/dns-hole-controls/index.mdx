---
title: "DNS Hole Controls"
description: "Control PiHole or AdGuard from your dashboard."
hide_title: true
---

import { WidgetInteractionGuide } from "@site/src/components/widgets/interactions";

import { WidgetHeader } from "@site/src/components/widgets/header";
import { AddingWidget } from "@site/src/components/widgets/adding";
import { WidgetConfig } from "@site/src/components/widgets/configuration";
import { WidgetIntegrations } from "@site/src/components/widgets/integrations";
import { IconAd } from "@tabler/icons-react";
import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { dnsHoleControlsWidget } from ".";
import { adguardHomeIntegration } from "@site/docs/integrations/adguard-home";
import { piHoleIntegration } from "@site/docs/integrations/pi-hole";
import { technitiumDnsIntegration } from "@site/docs/integrations/technitium-dns";

<WidgetHeader widget={dnsHoleControlsWidget} categories={["DNS Hole"]} />

This widget lets permitted users enable, disable, or temporarily pause one or more DNS services. It shares the summary query and cache with DNS Hole Summary. Advanced view exposes every selected source, while compact layouts reduce controls according to both widget width and height.

### Screenshots

import bigSize from "./img/big.png";
import smallSize from "./img/small.png";

<img src={bigSize} alt="dns-hole-controls big-size with-toggle-all-buttons" width={256} height={128} />
<img src={smallSize} alt="dns-hole-controls small-size no-toggle-all-buttons" width={128} height={128} />

### Supported Integrations

<WidgetIntegrations
  items={[
    {
      integration: adguardHomeIntegration,
    },
    {
      integration: piHoleIntegration,
    },
    {
      integration: technitiumDnsIntegration,
    },
  ]}
/>

### Interactions

- Enable or disable one DNS service by clicking its status badge.
- Temporarily disable one service with the clock action.
- Apply an action to all available services from the top action row. Homarr keeps successful results visible and reports how many sources failed.
- Interaction controls only appear when the current user has permission for that integration.

<WidgetInteractionGuide />

### Adding the widget

<AddingWidget />

### Configuration

<WidgetConfig configuration={dnsHoleControlsWidget.configuration} />
