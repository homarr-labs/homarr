---
title: "Timetable"
description: "Displays departure times for a station."
hide_title: true
---

import { WidgetInteractionGuide } from "@site/src/components/widgets/interactions";

import { AddingWidget } from "@site/src/components/widgets/adding";
import { WidgetConfig } from "@site/src/components/widgets/configuration";
import { WidgetHeader } from "@site/src/components/widgets/header";
import { timetableWidget } from ".";

<WidgetHeader widget={timetableWidget} categories={["Timetable"]} />

The Timetable widget provides real-time departure times for a selected station. Compact mode keeps configured titles, line names, and platform fields visible while adapting dense rows to the available space.

### Screenshots

import trainDepartures from "./img/train-departures.png";
import streetDepartures from "./img/street-departures.png";

<div className="flex gap-2">
  <img src={trainDepartures} alt="timetable-widget-train-departures" width={128 * 2} />
  <img src={streetDepartures} alt="timetable-widget-bus-and-tram-departures" width={128 * 2} />
</div>

<WidgetInteractionGuide />

### Adding the widget

<AddingWidget />

### Configuration

<WidgetConfig configuration={timetableWidget.configuration} />

For security, all custom base URLs must resolve to a public IP address whenever Homarr uses them. Private and local hosts are rejected.

If a timetable refresh fails after departures have loaded, the widget keeps the previous departures visible and marks them as stale. The advanced view also shows their last update time.
