---
title: "Network Controller Summary"
description: "Displays the summary of a Network Controller"
hide_title: true
---

import { WidgetInteractionGuide } from "@site/src/components/widgets/interactions";

import { unifiControllerIntegration } from "@site/docs/integrations/unifi-controller";
import { AddingWidget } from "@site/src/components/widgets/adding";
import { WidgetConfig } from "@site/src/components/widgets/configuration";
import { WidgetHeader } from "@site/src/components/widgets/header";
import { WidgetIntegrations } from "@site/src/components/widgets/integrations";
import { IconAd } from "@tabler/icons-react";
import TabItem from "@theme/TabItem";
import Tabs from "@theme/Tabs";
import { networkControllerSummaryWidget } from ".";

<WidgetHeader widget={networkControllerSummaryWidget} categories={["Network controller"]} />

Shows whether your WAN, internet, Wi-Fi, and VPN are available. The compact board view keeps the familiar status list, including internet latency and the connected VPN-user count.

### Screenshots

import summaryCard from "./img/summary.png";

<img src={summaryCard} alt="network-controller-summary" width={128 * 1.5} height={128 * 1.5} />

### Supported Integrations

<WidgetIntegrations
  items={[
    {
      integration: unifiControllerIntegration,
    },
  ]}
/>

In advanced view, each controller gets its own information matrix showing source, last update, WAN and web status, latency, ping, web uptime, separate Wi-Fi and LAN user and guest counts, and connected VPN users.

<WidgetInteractionGuide />

### Adding the widget

<AddingWidget />
