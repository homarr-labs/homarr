---
title: "App"
description: "Embeds an app into the board."
hide_title: true
---

import { AddingWidget } from "@site/src/components/widgets/adding";
import { WidgetConfig } from "@site/src/components/widgets/configuration";
import { WidgetHeader } from "@site/src/components/widgets/header";
import { appWidget } from ".";

<WidgetHeader widget={appWidget} categories={["Links", "Apps"]} />

The App widget opens an application or website and can show whether Homarr can reach it. Configure the target and status
check from the tile's edit actions. Very small tiles shorten the title and description to leave room for the icon;
enlarge the tile to show more text.

### Adding the widget

<AddingWidget />

### Configuration

<WidgetConfig configuration={appWidget.configuration} />
