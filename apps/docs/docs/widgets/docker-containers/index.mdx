---
title: 'Docker stats'
description: 'Stats of your containers'
hide_title: true
---

import { WidgetHeader } from '@site/src/components/widgets/header';
import { AddingWidget } from '@site/src/components/widgets/adding';
import { WidgetConfig } from '@site/src/components/widgets/configuration';
import { WidgetIntegrations } from '@site/src/components/widgets/integrations';
import { IconAd } from "@tabler/icons-react";
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { dockerContainersWidget } from '.';
import { dockerIntegration } from '@site/docs/integrations/docker';


<WidgetHeader
  widget={dockerContainersWidget}
  categories={['Containers']}
/>

This widget displays the resource usage of your Docker containers and allows you to manage them.

:::info
This widget can only be used with administrator privileges.
:::

### Screenshots

import dockerTable from './img/table.png';

<img src={dockerTable} alt="docker-stats table docker-containers" width={128 * 3} height={128 * 2} />

### Supported Integrations

<WidgetIntegrations items={[{
  integration: dockerIntegration,
}]} />

### Interactions

- Start a container by clicking on the play icon.
- Stop a container by clicking on the stop icon.
- Restart a container by clicking on the refresh icon.

### Adding the widget

<AddingWidget />
