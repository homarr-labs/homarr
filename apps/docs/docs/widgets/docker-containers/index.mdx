---
title: "Docker stats"
description: "Stats of your containers"
hide_title: true
---

import { WidgetHeader } from "@site/src/components/widgets/header";
import { AddingWidget } from "@site/src/components/widgets/adding";
import { WidgetConfig } from "@site/src/components/widgets/configuration";
import { dockerContainersWidget } from ".";

<WidgetHeader widget={dockerContainersWidget} categories={["Containers"]} />

Displays container resource usage and management actions for Docker environments configured through environment
variables.

:::info
This widget does not use an integration from **Management → Integrations** and is restricted to administrators.
:::

### Screenshots

import dockerTable from "./img/table.png";

<img src={dockerTable} alt="docker-stats table docker-containers" width={128 * 3} height={128 * 2} />

### Interactions

Column order and width are saved per widget. These controls are unavailable while the board is in edit mode.

Each container row has an action menu, accessible via the three-dot icon or by right-clicking the row, with the following options:

- **View logs** - opens a dedicated terminal page streaming the container's logs
- **Start** / **Stop** / **Restart** - manage the container's state
- **Remove** - permanently remove the container after a second confirmation click
- **Add to Homarr** - create a bookmark app from the container

### Adding the widget

<AddingWidget />

### Configuration

<WidgetConfig configuration={dockerContainersWidget.configuration} />
