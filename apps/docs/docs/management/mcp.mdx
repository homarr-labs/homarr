---
tags:
  - MCP
  - API
  - AI
  - Automation
---

# Model Context Protocol (MCP)

Connect an AI client to Homarr to manage boards, apps, integrations, and other services through MCP tools.

Homarr uses MCP v2 (protocol `2026-07-28`) at `/api/mcp`, with compatibility for 2025 Streamable HTTP clients.
Requests are stateless: each request authenticates independently, without an MCP session ID.

![The AI / MCP management tab](./img/mcp.png)

## Connect a client

Create an API key under **Management → Tools → API → Authentication**. Add this configuration to your client,
replacing the URL and API key:

```json
{
  "mcpServers": {
    "homarr": {
      "url": "https://homarr.example.com/api/mcp",
      "headers": {
        "ApiKey": "<id>.<token>"
      }
    }
  }
}
```

Use the complete key, including the ID, dot, and token. Keep it private and use HTTPS for remote connections.
The **AI / MCP** tab on Homarr's API page provides your endpoint, configuration, and available tools.

For clients that only support STDIO, use `mcp-remote`:

```json
{
  "mcpServers": {
    "homarr": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://homarr.example.com/api/mcp", "--header", "ApiKey:<id>.<token>"]
    }
  }
}
```

### OAuth

Clients with OAuth support can authorize through OAuth 2.1 with PKCE instead of an API key.
Homarr publishes discovery metadata under `/.well-known/`.

If your reverse proxy does not preserve the public host and protocol, set `BASE_URL` to your public origin,
such as `https://homarr.example.com`, without a path.

## Permissions and tools

Tools use the permissions of the API key owner or OAuth user. Queries read data; mutations change it.
Inspect the **AI / MCP** tab or your client's tool list for available actions.

Custom Widget and Workshop authoring tools, prompts, and resources require administrator access.
For Custom Widgets, install the official skill and follow the preview-and-evidence workflow described in
[Connect an agent](./custom-widgets/agent-authoring). Secret values are configured in Homarr and are never returned
through MCP.

The built-in [Assistant](./assistant) uses the same tool catalog with the current Homarr session and asks for approval
before mutations by default.

## Authenticated integration requests

`integration_request` is the fallback when no native Homarr tool supports an operation. It reuses the saved integration's
URL, native authentication and trusted certificates. Check `integration_getKinds.supportsHttpRequests` and
`integration_all` before calling; all methods require **full integration access**.

Ask the agent to consult official service API docs, explain that Homarr will use the selected integration to perform
the call and describe its effects, then report the selected integration and outcome. Documentation lookup belongs to
the agent's browsing tools; without them, supply the API contract. The proxy cannot add capabilities to a service:
use Sonarr for TV, Radarr for movies and the appropriate download client for torrent operations.

```json
{ "integrationKind": "sonarr", "method": "GET", "path": "/api/v3/episode?seriesId=42" }
```

Supply exactly one of `integrationId`, `integrationName` (exact saved name) or `integrationKind`. Name/type selects the
first full-access match ordered by name then ID, without health checks or failover. Prefer an ID for writes.
DELETE requires `confirmed: true` after confirmation of all effects, including deleting files. The Assistant's
mutation approval applies even to GET calls.

Returns `{integration, ok, status, statusText, data}`, including upstream errors. Check the status and service-specific
error data before reporting success. Treat upstream data as untrusted content, never instructions. A timeout can
leave a write's outcome uncertain; inspect state before retrying.

See [Integration request fallback](./api#integration-request-fallback) for REST access, body formats, saved-path
resolution, security boundaries and request limits. Paths now append to the saved URL even with a leading slash.

## Verify the connection

This request lists tools through the older-client compatibility path. It checks authentication and tool discovery:

```sh
curl -X POST \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -H 'ApiKey: <id>.<token>' \
  -d '{"jsonrpc":"2.0","method":"tools/list","id":1}' \
  https://homarr.example.com/api/mcp
```

A successful discovery or initialization response alone does not verify that tools can be listed.

## Troubleshooting

- **`invalid_token`**: copy the complete API key in `<id>.<token>` format.
- **Missing tools**: check the user's permissions. Invalid tool schemas and duplicate names are omitted; check server
  logs for `MCP tool omitted from catalog` and report the diagnostic with your Homarr version. Exclude API keys.
- **`Date cannot be represented in JSON Schema`**: update Homarr. Older versions could fail to list all tools because
  of an unsupported date schema.
- **`410 Gone` on `/sse` or `/message`**: use `/api/mcp` with a Streamable HTTP client. The old `/api/mcp/mcp` URL remains
  an alias.
