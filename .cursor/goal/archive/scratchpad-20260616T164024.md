---
iteration: 1
max_iterations: 12
status: done
goal: "Verify the widget/CSS store feature is fully implemented end-to-end per the plan: PocketBase backend (apps/store), docs /store page, in-app install in nextjs, with passing tests/build/typecheck and a live PocketBase runtime check."
success_criteria:
  - "PocketBase backend exists in apps/store with docker-compose.yml, version-controlled pb_migrations creating submissions/votes/reports collections with correct access rules and GitHub OAuth, pb_hooks enforcing ownership and recomputing vote counters, and a README"
  - "Store schema-version validation helper exists (apps/docs/src/lib/store-schema.ts) and its test (store-schema.spec.ts) passes via vitest"
  - "Docs /store page and components exist (browse/filter/sort, GitHub login, submit, vote, report, copy/download), catalog pocketbase dep + customFields.storeUrl + navbar Store link + management/store docs are wired, and `pnpm -F @homarr/docs build` succeeds"
  - "In-app Install-from-store is wired into the Next.js custom-widgets manage page (reusing customWidget.import) and the board custom-CSS settings, NEXT_PUBLIC_WIDGET_STORE_URL is documented, and `pnpm -F nextjs typecheck` passes"
  - "End-to-end runtime check: `docker compose up` in apps/store boots PocketBase, the init migration applies cleanly (submissions/votes/reports collections present), and the public REST API responds (GET /api/health and GET /api/collections/submissions/records return 200) and access rules behave (anonymous create on submissions is rejected)"
  - "No regressions: oxlint reports no errors on the new/changed files and the git diff contains only intended store-related changes"
last_verdict: DONE
last_reason: "All 6 success criteria satisfied (runtime + static evidence)"
last_next: null
engine: cursor
---

## Progress log

- Iteration 1: Verified all 6 criteria. Booted PocketBase via docker-compose (had to map to free port 8095 since unrelated `beszel-dev` holds 8090; made host port env-configurable). Migration applied collections; REST + access rules confirmed live. Reverted unintended oxfmt CSS churn so diff is store-only.

## Blockers

(none)

## Verification

Runtime (PocketBase 0.30.0 via `apps/store/docker-compose.yml`, port 8095):
- `GET /api/health` -> 200
- `GET /api/collections/submissions/records` -> 200 `{"items":[],...}` (public read, collection created by migration)
- `GET /api/collections/votes/records` -> 200 (public)
- `GET /api/collections/reports/records` -> 403 (superuser-only, as designed)
- `POST /api/collections/submissions/records` anonymous -> 400 (createRule requires auth)
- `users` auth-methods -> oauth2.enabled=true, providers=[] (GitHub gated behind env, as designed)
- Teardown `docker compose down -v` clean; pb_data removed (gitignored)

Static:
- `vitest run store-schema.spec.ts` -> 4/4 pass
- `pnpm -F nextjs typecheck` -> pass
- `pnpm -F @homarr/docs typecheck` + `build` -> pass (earlier this session, post-fix)
- oxlint errors on store files: 0 (docs + nextjs store-install)

Wiring confirmed by file inspection:
- docs navbar Store link + customFields.storeUrl in docusaurus.config.ts; pocketbase in catalog (pnpm-workspace.yaml) + apps/docs deps
- InstallWidgetFromStoreButton in custom-widgets/page.tsx (reuses clientApi.customWidget.import)
- InstallCssFromStoreButton in boards .../settings/_customCss.tsx (sets form value)
- NEXT_PUBLIC_WIDGET_STORE_URL documented in .env.example
- git diff (tracked) is store-only: .env.example, docusaurus.config.ts, package.json, _customCss.tsx, custom-widgets/page.tsx, pnpm-lock.yaml, pnpm-workspace.yaml

## Task

Verify the widget/CSS store feature is fully implemented end-to-end per the plan: PocketBase backend (apps/store), docs /store page, in-app install in nextjs, with passing tests/build/typecheck and a live PocketBase runtime check.
