# Impact — full treatment

43-second, 60 fps iteration applying the approved layered UI treatment across the entire video.

- Custom Widgets keeps the editor-to-real-dashboard reveal.
- Drag/resize shows the real 60 fps capture in board context and a synchronized close-up, with action-specific captions.
- Sidebars pairs the scrolling recording with enlarged crops of the actual app launchers.
- Advanced widgets combines the compact Downloads tile, the recorded Shift opening, and the one-second hold indicator.
- Assistant retains the approved real draft, starter actions, and approval controls.
- Onboarding brings the actual stage rail, defaults, and address-pattern controls forward as separate readable crops.
- Header studio brings its actual zones and available controls forward, with sequential focus rings.
- Ending retains twin confetti and adds subtle product panels behind the logo.

All new crops derive from the existing real local captures or the explicitly disclosed Header studio release screenshot. The drag context is another crop of the original 60 fps recording, at the same 1.12× speed as its close-up. No interaction was synthesized. The initial compact Downloads still and expanded recording use the same seeded demo, captured at different times.

Existing capture limitations remain in `../impact-dashboard/README.md` and `../impact-refined/README.md`. Header studio is not a live local recording; the local demo image predates that feature. The custom widget shows a real tile in local layout-editing context. Assistant shows a draft and controls rather than a fabricated answer.

Sound: original music with 49 TIKS accents, including the newly layered controls. Loudness target −17 LUFS. Audio is measured rather than subjectively auditioned. Exact cue timings live in `sound-cues.json`.

Final verification: 1920×1080, 2580 frames, 60 fps, 43 seconds. Composition checks passed with zero runtime/layout/motion errors and 27/27 contrast checks; the existing timeline-density advisory remains. Encoded audio: −17 LUFS integrated, −2.6 dBFS true peak. Tailscale chapter playback reported zero dropped frames.

Delivered through native Taildrop to obsidian (100.105.184.54) as `homarr-v2-impact-full-60fps.mp4`. HTTP 200 acknowledged 21,180,333 bytes. Transfer evidence is in `taildrop.log` and the response files.
