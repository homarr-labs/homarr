# Local product captures — 2026-09-17

Fresh isolated container homarr-film-capture from existing homarr:simplified-demo, served only on localhost:7589, DEMO_MODE=true and DEMO_READ_ONLY=false. Main instance untouched; no image build.

- workbench.png: own browser capture at 1600x1000, cropped to settings/API source + rendered Pokédex preview. Seed widget /manage/custom-widgets/edit/seed-pokedex. Test and preview completed a real PokeAPI request; opened Bulbasaur detail through the actual widget control.
- custom-preview.png: tight crop of the visible #preview panel after that request succeeded; native element capture was rejected because sticky positioning misframed it.
- advanced-downloads.png: direct screenshot of the actual advanced Downloads modal, populated with demo integration data.
- downloads-tile.png / timer-tile.png: crops from own local board screenshot.
- assistant.png: direct screenshot of local Assistant controls. Provider is mock; no genuine agent execution is claimed. Its canned response was excluded.
- drag.mp4: newly recorded real calendar/weather swap in writable demo. Cropped to affected row, slowed to 2/3 speed with short endpoint holds. No synthesized movement.

Secondary Workshop, responsive-layout and Docker imagery in the video still comes from the release blog; the four primary feature sections now use local captures.
