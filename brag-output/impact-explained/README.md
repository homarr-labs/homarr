# Impact — explained

43-second iteration of Impact. Feature order: Custom Widgets v2 (0–8), real drag and resize (8–16), app sidebars (16–20), advanced widgets (20–25), Assistant (25–30), onboarding (30–35), header configuration (35–40), closing (40–43). Each feature has a concrete description. Removed the earlier Drag/Check/Commit slogan.

## Evidence

- New /tmp/homarr-editor-full.webm recording, archived in ../capture-evidence/: actual weather tile movement and a real handle-driven shrink/expand in the isolated demo. Converted directly to MP4; no GIF intermediate and no synthetic drag/resize.
- New sidebar WebM: actual board scroll with the app rail beside it; cropped and encoded to MP4.
- New onboarding capture from isolated homarr-film-onboarding, a fresh local instance on localhost:7590. No Docker socket was mounted. Capture shows Essentials; descriptions cover the actual six-step workflow.
- Custom Widget, advanced Downloads and Assistant controls use the previous verified local captures. Assistant provider is mock; no fake tool response shown.
- Header studio is a tightly cropped release-blog screenshot. The existing local image predates Header studio, so this section is not represented as a new local recording.

## Sound

Original deterministic electronic score generated by soundtrack.py, with synthesized pickup/drop tones, softer transition noise, and a closing accent. No third-party music. Mastered to -18 LUFS, measured true peak -4.4 dBFS. No narration. Cue timing is editorial rather than audio recorded from Homarr.

## Validation

Hyperframes check: zero errors/warnings; 28/28 contrast checks passed. Decorative transition overflow is informational. Render and final frame/audio review results are recorded separately after export.

Hosted by the existing Tailscale range server at http://100.111.30.70:3019/impact-explained/ .

Final export verification: 43 seconds, 1920×1080, 30 fps, 1290 frames. Composition checks passed with no errors or warnings. Encoded audio measures −18.0 LUFS integrated and −4.2 dBFS true peak. The Tailscale player loads the final export with sound enabled; playback and seeking were verified in the browser. Audio was measured, not subjectively auditioned.
