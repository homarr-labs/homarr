# Hyperframes composition brief: Homarr 2.0

Implement `brag-plan.md` and `prompt.md` as an editable 1920×1080, 80-second, 30fps composition in `composition/`. Output `brag.mp4`, `brag.jpg`, `share-copy.txt` and a reviewable Studio preview. The longer duration is intentional: the user's full-release scope supersedes Brag's teaser default.

Use actual release images under `apps/docs/blog/2026/09-03-homarr-2.0/img/`, actual integration icons from `source-inventory.json`, the real logo under `apps/docs/static/img/logo.svg`, and source-faithful visual card reconstructions. Use the integration marquee's three-column, ten-degree tilt as the motion reference. Catalog search found a generic perspective marquee; its horizon/3D treatment does not match the onboarding columns, so use a deterministic custom translation.

Identity: Inter; `#242424` stage; `#f8f9fa` primary text; `#fa5252` accent; `#ced4da` secondary text. Source screenshots retain their own color. Use full-width product evidence, short headlines, staggered card arrivals, purposeful cursor motion and readable resting states. No generic SaaS language, placeholder logos, fabricated benchmarks, narration or fake tool-execution claims.

Use one registered paused timeline, timed scene clips and local assets. Keep separate inner wrappers for camera motion. Name and verify the drag subject's pickup, travel, destination and release poses, and the resized tile's final state. Marquees are intentionally clipped at their viewport boundaries. All 55 integration icons and all 60 widget kinds must appear during their scenes.

Music is Brag Business Moves vol. 12; copy it locally with its attribution. Select sparse low-risk interface clicks from Brag SFX after visual timing exists. Use the bundled cue metadata for the 6s/18s beats; natural timing takes priority elsewhere. Extract frequency bands through hyperframes-creative for restrained glow modulation. No narration.

Validation: run Hyperframes check with runtime/layout/contrast and scene snapshots, inspect each scene plus drag/resize poses, then open the persistent Studio preview. Follow the skill's preview approval gate before final encoding. After encoding verify duration, dimensions, audio and selected poster at frame zero with ffprobe and extracted frames. Keep source manifests and commands alongside the composition for reproducibility.

The user approved rendering and further iteration. The second pass enlarges the real Workshop/workbench views, adds source-grounded widget control and countdown motion, and shows branding and permissions through real source screenshots. Both full renders retain the original 80-second structure and catalog coverage.
