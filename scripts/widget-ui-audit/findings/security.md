# Security

- Board: `widget-ui-audit-security`; authenticated demo capture at 1080p, 2k, and MacBook Pro.
- Coverage: 4 widget kinds × 10 sizes × 3 viewports, 123 PNGs including board shots; zero recorded overlaps and no error markers.
- Runtime state: all 40 widgets mounted and ready at every viewport with no loading indicators recorded.
- Representative crops: `screenshots/security/desktop-1080p/*__01__firewall__1x1.png` and `screenshots/security/macbook-pro/*__40__ups__5x5.png`.

The 1x1 firewall crop keeps both gauges and percentages visible, but the integration label is shortened to `Demo...` and the header controls are tightly packed. The 5x5 UPS crop cleanly lays out two device cards with battery rings, runtime, load bars, voltage, and state badges, followed by a large empty lower region.
