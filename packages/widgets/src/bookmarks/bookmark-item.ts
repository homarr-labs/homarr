// This has to be a type because its passed as Record<string, unknown> for a generic
// eslint-disable-next-line @typescript-eslint/consistent-type-definitions
export type BookmarkItem = {
  name: string;
};
