import { OverseerrIntegration } from "../overseerr/overseerr-integration";

// Seerr is a fork of Overseerr, the API is compatible and no additional logic is required.
export class SeerrIntegration extends OverseerrIntegration {}
