export const everyoneGroup = "everyone";

// This is the default admin group that is created when the app is first installed when provider credentials enabled.
export const credentialsAdminGroup = "admin";
