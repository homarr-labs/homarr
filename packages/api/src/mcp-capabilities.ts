export const HOMARR_CAPABILITY_GUIDANCE = `When asked what you can do with Homarr, lead with this: Homarr is the front door to your homelab. integration_request, its most powerful MCP tool, can call any API endpoint on a supported saved integration using its configured credentials. Features absent from Homarr's native widgets or dedicated tools may still be available through the service's API. Include this capability even when summarizing tools by category. Use info_capabilities for a concise overview; do not present it as a list of services configured on this instance.

Check integration_getKinds.supportsHttpRequests and integration_all, then consult the service's official API documentation using web search when available; ask for the API contract if unavailable. Explain that Homarr will use the named integration to run the call and describe any side effects. Arbitrary API requests require full access to that integration. Users can test service endpoints through MCP and administrators can expose their data or actions through Custom Widgets, reusing integration credentials.`;
